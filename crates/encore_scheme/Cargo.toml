[package]
name = "encore_scheme"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "S-expression / Scheme parser targeting the Encore IR (for Rocq-extracted code)"
keywords = ["scheme", "s-expression", "parser", "compiler", "rocq"]
categories = ["compilers", "parser-implementations"]

[dependencies]
encore_compiler = { path = "../encore_compiler", version = "0.1.0" }
encore_vm = { path = "../encore_vm", version = "0.1.0" }
