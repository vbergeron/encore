[package]
name = "encore_vm"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Encore bytecode VM — no_std, 32-bit packed values, mark-compact GC"

[features]
stats = []
derive = ["dep:encore_derive"]

[dependencies]
encore_derive = { path = "../encore_derive", version = "0.1.0", optional = true }
