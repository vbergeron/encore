[package]
name = "encore_vm"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
readme.workspace = true
description = "Encore bytecode VM — no_std, 32-bit packed values, mark-compact GC"
keywords = ["vm", "bytecode", "no-std", "functional", "embedded"]
categories = ["no-std", "embedded"]

[features]
stats = []
derive = ["dep:encore_derive"]

[dependencies]
encore_derive = { path = "../encore_derive", version = "0.1.2", optional = true }
