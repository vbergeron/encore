[package]
name = "encore_compiler"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
readme.workspace = true
description = "Encore compiler — CPS IR pipeline for the Fleche functional language"
keywords = ["compiler", "cps", "bytecode", "functional", "ir"]
categories = ["compilers"]

[dependencies]
encore_vm = { path = "../encore_vm", version = "0.1.2" }
